
const permissionDefault = {
    read: false,
    create: false,
    update: false,
    delete: false
}

export const defaultPermissions = {
    transfers: permissionDefault,
    transactions: permissionDefault,
    finances: permissionDefault,
    users: permissionDefault, 
    roles: permissionDefault,
}